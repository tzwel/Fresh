Fresh provides useful aliases for common JavaScript and its own methods.

## Alias table
| **Code**                  | **Alias** |
|---------------------------|-----------|
| document.querySelectorAll | $qsa      |
| document.querySelector    | $qs       |
| window                    | $var      |

* Learn more about the `$var` alias [here](per route variables.md).